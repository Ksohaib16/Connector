export const Button = ({ text }: { text: string }) => {
  return <button className=
  "btn bg-[#3874c9] w-full h-[3rem] mt-[0.5rem] rounded-[0.65rem] text-[#FFFF] mb-[2rem]">
    {text}
    </button>;
};
